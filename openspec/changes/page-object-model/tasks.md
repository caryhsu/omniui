## 1. Core Implementation

- [ ] 1.1 Create `omniui/page.py` with `OmniPage` base class (`__init__(client)` + `locator(**selector)`)
- [ ] 1.2 Export `OmniPage` from `omniui/__init__.py`

## 2. Tests

- [ ] 2.1 Add `tests/test_page_object.py` with example usage (subclass, call client method, use locator shorthand)
- [ ] 2.2 Run `python -m pytest tests/` — confirm all pass

## 3. Wrap-up

- [ ] 3.1 Update `ROADMAP.md` / `ROADMAP.zh-TW.md` — mark Page Object Model as `[x]`
- [ ] 3.2 `git commit` + `git push`
